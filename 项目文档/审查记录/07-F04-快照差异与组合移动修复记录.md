# F04：Snapshot 差异重复写入与组合移动修复记录

日期：2026-09-06

**F04 已实施、自动验证通过，等待用户复核；整体 NO-GO 保留。**

F01/S01、F02、F03 保持其既有“已交付、自动验证通过、等待用户复核”状态，不视为用户已确认。N00–N13 仍为实现完成。本轮仅实施 F04，完成后停止，不进入 F05/U04。

## 1. 范围与本轮实际基线

依据当前 V2 第 5 节、原审计 F04、W01 启动核查和 F01/S01、F02、F03 最新修复记录，结合当前 Catalog、FTS、IndexManager、Scanner 与测试。旧报告行号只用于定位，下文引用最终源码行号。

开始时 HEAD 为 `15b25b14b1608638b3ba779de6daf2182bfcf585`。实际工作区已有上一轮 F03 的 7 个未提交文件：README、Hook/Loadout 源码、Hook/N13 测试、未跟踪的 Loadout 资格测试及 F03 记录。本轮重新采集 **128 个** tracked / 非忽略 untracked 文件的 SHA-256 基线，未沿用上一轮数量，未把这些已有变更当作 F04 产出。

基线与原始日志目录：`/var/folders/3k/g7_mt00n1rdfxx__dj6vnnk00000gn/T/codex-f04-remediation-3rpx2rkt/`，包含 `baseline.json`、`initial-status.txt`、`head.txt` 和本轮命令日志。正式回归在仓库中，不依赖原审计临时脚本。

单 Agent 串行；没有 commit/push/reset/clean/stash，没有升级依赖、修改锁文件、生产数据库 Schema、个人配置或真实知识。测试全部使用独立临时 Repository、配置、SQLite 和 loopback 临时端口；触发器只存在于故障测试的隔离 SQLite。没有引入自动 rebuild、恢复 CLI、全局异常过滤或新平台。

## 2. 根因、差异分类与最小算法

修复前 `calculateDifference` 遍历旧 Catalog 行，通过 `sameIdAsset ?? assetsByPath.get(row.filePath)` 寻找新 Asset。删除 A 再把 B 移到 A 路径后，旧 A 经路径匹配 B，旧 B 经 ID 匹配 B，得到 `changed=[B,B]`。`consumedAssetIds` 只影响 added，不能阻止 changed 重复写入。Scanner 生成的 Snapshot 合法，错误产生在差异计算。

原有消费者 `apps/server/src/asset/catalog.ts:112` 已在单一 SQLite 事务内先删除全部受影响旧行，再插入新行；`:264` 按旧 rowid 删除 FTS/Catalog，`:278` 普通 INSERT Catalog 后以 lastInsertRowid 插入 FTS。因此无需改写事务、约束或状态体系，只需修正差异集合。

本轮生产修改仅在 `apps/server/src/asset/catalog.ts:354`：

1. 构造新 Snapshot 与旧 Catalog 的 ID 映射。
2. 遍历新 Snapshot 的唯一 ID：无旧身份则 added；同 ID 且现有 `sameCatalogValues` 全字段相等则 unchanged；否则 changed，并记录该 ID 的旧 changedRow。
3. 独立遍历旧行：新 Snapshot 已没有该 ID 时，按原路径诊断规则分类为 invalidatedRows 或 removedRows；不再以被其他身份占据的旧路径认领新 Asset。
4. 复用现有事务，先删除 removed/invalidated/changed 的全部旧 FTS 和 Catalog 行，再对 changed/added 各新身份写入一次。两个删除分类与 changedRows 互斥；changed/added 互斥；unchanged 完全不写。

Scanner 仍负责完整 Snapshot 与重复 ID 全部排除，差异函数接收的是该合格集合；没有新增“重复 ID 取一个获胜者”的 Scanner 行为。复杂度保持按新旧集合线性处理，没有全库清空再重建。

| 分类 | 修复后的准确含义 | 与旧统计的关系 |
|---|---|---|
| added | 新 Snapshot 有、旧 Catalog 无的 Asset ID | 同路径换新 ID 现在记 added；旧算法把它计为 changed |
| changed | 新旧均有该 ID，但路径、Hash、类型、范围、Workspace、标题、摘要、大小或 mtime 等现有比较字段有变化 | 同 ID 移动仍是 changed，每个新身份一次 |
| unchanged | 同 ID 且现有比较字段全部相等 | 保留 rowid、indexed_at，完全不写 |
| invalidated | 新 Snapshot 无该旧 ID，且诊断命中它的旧路径 | 保留原诊断路径分类规则 |
| removed | 新 Snapshot 无该旧 ID，且无上述路径诊断 | 消失身份即使旧路径被另一身份复用，也计 removed |

因此，“同路径换新 ID”是 removed=1、added=1，“同 ID 移动”是 changed=1。该统计调整遵循本轮身份要求，不合并或转移历史。`CatalogSyncResult` 字段结构不变；当前生产调用方是 IndexManager，HTTP/MCP 没有返回这组同步统计。现有 `asset-index.test.ts:158` 的单个 replaced 统计断言相应改为完整五字段断言，其旧身份消失、路径正确、旧 FTS 消失及新 FTS 命中的原断言全部保留。

现有 `sameCatalogValues`、兼容导出、Schema 和一致性检查未清理。`IndexManager:207` 的完整 Snapshot gate、`:225` 的成功状态更新、`:231` 的真实失败处理均未改动；不完整扫描仍不应用删除，失败不伪报 READY。

## 3. 原反例先红后绿

新增正式回归：`apps/server/test/snapshot-difference.test.ts`，采用项目现有 `CODEX_MEMORY_OS_TEST_DIST` 约定。A/B 身份由统一生成器创建，与原报告 ast301/ast302 等价；基础样本为 `a.md=A`、`b.md=B`、`c.md=C`，C 始终合法不变。三文件轮换另加 D，C 继续作对照。

测试准备真实 Task、Turn Binding、Resolve 后 Loadout 和 Usage，包含 A、B 的历史关联。准备完成后采样全行基线。未启动 watcher，文件操作完成后才调用真实 Scanner 和正常 synchronize，不注入虚构 Snapshot、不 mock SQL 或资格判断、不依赖 sleep。

原组合反例 `test/snapshot-difference.test.ts:33`：删除 a.md，rename b.md 到 a.md，并逐字节比较 B 原文件；最终扫描 complete=true、诊断为空。随后正常 synchronize。修复前真实结果：

```text
synchronize() = null
indexState = DEGRADED
INDEX_UPDATE_FAILED:
  Catalog/FTS transaction failed and was rolled back:
  UNIQUE constraint failed: asset_catalog.file_path
catalogCount = 3; ftsCount = 3
rebuildRequired = false; watcherState = NOT_STARTED
再次 synchronize() = null，仍为同一唯一约束错误与 DEGRADED
catalogAndFtsRolledBack = true; runtimeUnchanged = true
```

日志 `before.log` 中失败断言为 `A complete valid final Snapshot must synchronize without rebuild`，位于 `test/snapshot-difference.test.ts:70`。两次失败都检查 Catalog 完整行、FTS rowid 集合和旧 token 命中保持基线，同时 Task/Binding/Loadout/Usage 全行不变；并非只比较三行数量。

修复前正式运行 **9 项，7 pass / 2 fail，exit 1**：一项是上述真实组合移动故障；另一项是同路径新 ID 在旧算法下统计为 changed=1，与本轮要求的 added/removed 身份分类不符。后者不是另一项事务故障。路径交换、轮换和 SQL 回滚等原本通过的对照不被算成新增修复贡献。

修改生产差异算法后，**同一测试文件、同一场景与断言 9/9 通过**，原组合反例一次正常 synchronize 返回 `{added:0, changed:1, invalidated:0, removed:1, unchanged:1}` 并 READY；B 只写入一条，FTS 关联正确。再同步同一 Snapshot 返回全部 unchanged，行数据不改。没有调用 rebuild 或整库重置。

正式基线前有两次测试夹具校正，日志保留：第一次误用不存在的 Binding 排序列，9 项均在准备阶段失败；按真实复合主键修正。第二次 6 pass / 3 fail，其中 SQL 回滚已成功，但 MCP gate 断言误用错误码；按现行 `ASSET_INDEX_UNAVAILABLE` 修正。两次均未修改生产代码，不作为 F04 反例证据，不将失败写成 PASS。

## 4. 覆盖矩阵与数据正确性

以下场景均在源码和 dist 模式执行；除合法空 Snapshot 必须删除 C 外，C 始终保留原完整 Catalog 行，包括 rowid、indexed_at，并持续命中 FTS。

| 场景 | 同步结果与关键验证 |
|---|---|
| A 删除 A，再将 B 移入 A 旧路径 | READY；A removed、B changed，各旧行先删，B 唯一写入；Hash/字节保持；A 历史 Usage missing=true，未转移给 B；实际 MCP Search/Read 成功 |
| B 两 ID 交换路径 | changed=2，C unchanged；一次同步无 file_path 唯一冲突，各身份对应新路径与原内容 |
| B 三 ID 轮换路径 | changed=3，C unchanged；通过仓库外同一隔离根的临时位置完成文件操作，最终 Snapshot 合法，Catalog/FTS 映射正确 |
| C 同路径更换 ID | added=1、removed=1；旧身份与旧 token 消失，新身份及其 token 可检索；旧运行历史保留并显示缺失关联 |
| C 同 ID 移到新路径 | changed=1；相同 ID/Hash 对应新路径，历史不变 |
| D 移动同时修改正文/摘要 | changed=1；新路径、新 Hash、标题/摘要/正文字段与真实文件一致；旧 token 不命中，新 token 在对应 FTS 列命中 |
| E 相同 Snapshot 再次同步 | 每个关键成功阶段都重复 synchronize；added/changed/removed/invalidated=0，所有合格项 unchanged；完整 Catalog/FTS 行不改，不持续 DEGRADED |
| F 重复 ID 全部排除、修复后恢复 | 两个 DUPLICATE_ASSET_ID 诊断，A 的所有重复文件均不入 Snapshot；旧 A invalidated，其他项不变；解除冲突后 A added 恢复，同一历史关联重新可见 |
| G Repository 或配置暂时不可用 | 真实扫描 incomplete；synchronize=null、DEGRADED、INCOMPLETE_SCAN_SNAPSHOT，lastSuccessfulScanAt 不变；旧索引、旧 FTS token、运行行完整保留；恢复后正常同步 READY |
| G 合法空仓库 | Repository/目录/配置存在，所有 Markdown 移除；完整空 Snapshot，removed=3，Catalog/FTS 为空且 READY；Task/Usage/Binding/Loadout 不删 |
| H 删除和部分写入之后真实 SQL 失败 | 见下一节；整个事务回滚、gate 拒绝；解除测试故障后正常同步 READY，实际 MCP Search/Read 成功 |

`test/snapshot-difference.test.ts:245` 将 Catalog 的完整 Asset ID 集合、类型、范围、Workspace、标题、摘要、路径、Hash、大小、mtime 与真实 Snapshot 逐字段比较，并校验路径/身份唯一。

FTS 为现行 contentless-delete trigram 表，读取其内容列是 NULL，不能靠内容列或行数证明索引正确。`:232` 检查双向 rowid 集合严格相等，并对所有旧/新 title、summary、body 唯一 token 执行真实 `asset_fts MATCH`，同时分别使用 `title : "token"`、`summary : "token"`、`body : "token"` 验证列对应与精确 Asset ID 集合。已删除/失效内容必须不命中，新增内容必须命中；这些断言独立于 Application Search 的任何回退路径。

`:260` 起再调用正常纯 Application Read/Search，核对当前 Markdown、Frontmatter、Hash、唯一目标和 FTS 策略。Read DTO 不包含路径，路径正确性由 Catalog/Snapshot 全字段比较证明，不为测试增加 DTO 字段。

## 5. 事务回滚、运行数据与 MCP 计数

H 使用测试侧 SQLite `BEFORE INSERT` 触发器（`:148`）：仅在两个受影响旧 Hash 已全部消失、Catalog 只含 C 与第一条新记录，且 FTS 已有两条记录时，在第二条插入处 `RAISE(ABORT, 'F04_AFTER_DELETE_AND_FIRST_FTS_INSERT')`。这证明失败发生在全部删除以及第一对新 Catalog/FTS 写入之后，不是事务开始前故障，也没有 mock SQL。

实际返回 null，诊断保留上述 marker，状态 DEGRADED、lastSuccessfulScanAt 不变。完整 Catalog 行、FTS rowid 及所有旧 token 恢复原状，新 token 不命中；没有半删除、半写入或孤立 FTS。故障期间真实 SDK `asset_read` 返回 ASSET_INDEX_UNAVAILABLE，Usage 不变。DROP 测试触发器后，普通 synchronize 立即成功，changed=2、unchanged=1，READY；不依赖 rebuild。源码与编译后两次均通过。

运行数据基线在初始 Resolve 与 Usage 准备之后采样（`:219` 起）：

- `SELECT * FROM task_loadout ORDER BY task_id`，包括原始 loadout_json 文本、数组顺序、ID、Workspace、状态、创建及更新时间；
- `SELECT * FROM task_turn_binding ORDER BY source_session_id, source_turn_id`，保留完整复合身份及 bound_at；
- `SELECT * FROM task_asset_usage ORDER BY usage_id`，包含 usage_id/task_id/asset_id、recall_count/read_count/used_flag、created_at/updated_at。

A/B 均有 Recall/Read 历史，A 另有 Used 标记。`:225` 在同步、回滚、纯查询后比较全部运行行；A 删除后仍保留原 usageId/计数/标记，按现有关联得到 assetMissing=true。

仅在上述不变性验证完成后，`:287` 单独执行真实 MCP SDK Search/Read。正常组合移动及故障恢复各执行一次，确认 READY gate 放行且返回正确当前内容；只允许被访问的 B Recall、Read 各增加 1，保留 usageId、createdAt、Used 标记等字段，updatedAt 按现行写入规则处理。其他 Usage 与 Task/Binding 完整不变。没有为了不变性断言而禁用 MCP 正常计数。

## 6. 命令、结果与未执行项

命令从仓库根执行，统一使用已有 Node.js **22.16.0** 与 pnpm **11.1.3**；每条命令的临时 PATH 前缀为 `/Users/hemu/.npm/_npx/78120b5db7e8f750/node_modules/node/bin:$PATH`，未改个人环境配置。

| 顺序 / 命令 | 实际结果与日志 |
|---|---|
| `pnpm --filter @codex-memory-os/server exec tsx --test --test-concurrency=1 test/snapshot-difference.test.ts`（夹具准备阶段两次） | exit 1；先 0/9、后 6/9 通过，原因见第 3 节；`fixture-error.log`、`before-with-fixture-error.log` |
| 同一命令，未修复生产代码的正式反例 | exit 1；7 pass / 2 fail，0 skipped；`before.log` |
| 同一命令，修复后 | exit 0；**9/9**，0 skipped；`targeted.log` |
| `pnpm --filter @codex-memory-os/server exec tsx --test --test-concurrency=1 test/asset-index.test.ts test/asset-scanner.test.ts test/asset-search.test.ts` | exit 0；**18/18**，0 skipped；`index-related.log` |
| `pnpm --filter @codex-memory-os/server exec tsx --test --test-concurrency=1 test/frontmatter-security.test.ts test/path-security.test.ts test/loadout-qualification.test.ts` | exit 0；F01/S01 42 + F02 20 + F03 15 = **77/77**，0 skipped；`f01-f03.log` |
| `pnpm --filter @codex-memory-os/server exec tsx --test --test-concurrency=1 test/mcp.test.ts test/rest.test.ts test/task.test.ts test/usage.test.ts test/n13-integration.test.ts` | exit 0；**36/36**，0 skipped；`runtime-related.log` |
| `pnpm test` | exit 0；Server 167、Hub 43、ID 4，共 **214/214**；Server/ID 0 skipped，Hub 43 全通过；`full-test.log` |
| `pnpm typecheck` | exit 0；全部 workspace 包通过；`typecheck.log` |
| `pnpm build` | exit 0；Server/Hub/ID 构建通过；`build.log` |
| `CODEX_MEMORY_OS_TEST_DIST=1 pnpm --filter @codex-memory-os/server exec tsx --test --test-concurrency=1 test/snapshot-difference.test.ts` | exit 0；**9/9**，0 skipped；`dist-f04.log` 明确 `F04_PRODUCTION_MODULE_ROOT …/apps/server/dist/`，包括原反例、交换/轮换、SQL 中途回滚和真实 MCP SDK |
| `CODEX_MEMORY_OS_TEST_DIST=1 pnpm --filter @codex-memory-os/server exec tsx --test --test-concurrency=1 test/path-security.test.ts test/loadout-qualification.test.ts` | exit 0；F02/F03 编译后 **35/35**，0 skipped，含真实 Hook 子进程；`dist-f02-f03.log` |
| `pnpm --filter @codex-memory-os/server exec node test-support/frontmatter-security-build-smoke.mjs` | exit 0；F01_BUILD_SMOKE status=ok，markerExists=false；`f01-build-smoke.log` |
| `pnpm --filter @codex-memory-os/server smoke:mcp:build` | exit 0；N08_BUILD_SMOKE status=ok，六工具主链路；`mcp-build-smoke.log` |
| `pnpm --filter @codex-memory-os/server smoke:n13:e2e:build` | exit 0；N13_E2E_BUILD_SMOKE status=ok，隔离 MCP/REST/Hook/Confirm/重启链路；`n13-build-smoke.log` |
| `git diff --check` | exit 0 |

F04 dist 模式的 harness 仍由 tsx 执行，Scanner/Catalog/IndexManager/Search/Task/Loadout/Usage/MCP 生产模块均从 dist 导入。没有将源码 helper 测试冒称构建产物验证，没有新增生产配置项。

未另执行独立 `smoke:rest:build`、`smoke:hub:build`、`smoke:asset-confirm:build`；相关行为由全量测试及既有 F01/N13 构建烟测覆盖，不将这些未执行命令写成 PASS。现有 N13 烟测中的隔离重建模拟不作为 F04 恢复证据；F04 的成功和故障恢复测试均只使用 synchronize。未做真实 Codex 生命周期、真实浏览器、真实知识迁移或生产环境验收。

## 7. 修改文件必要性与最终保护结果

| 本轮文件 | 必要性 |
|---|---|
| `apps/server/src/asset/catalog.ts` | 唯一生产修改：按新 ID 计算唯一写入集，独立计算消失旧身份，沿用原事务消费者 |
| `apps/server/test/asset-index.test.ts` | 将同路径新 ID 的统计断言改为 added/removed 身份语义，保留全部原内容和 FTS 安全断言 |
| `apps/server/test/snapshot-difference.test.ts`（新增） | 正式反例、完整矩阵、FTS 列命中、运行数据全行比较、故障触发器、SDK 和 dist 验证 |
| 本记录（新增） | 独立记录 F04 实施及等待用户复核状态，保留历史证据 |

与本轮 **128 文件** SHA-256 基线比较：**2 个既有文件发生上述授权修改，126 个逐字节未变，0 个缺失；新增 2 个文件**。上一轮 F03 的 7 个未提交文件完整保留原字节；F01/S01、F02、F03 正式安全测试均未调整、删除、跳过或弱化。原审计、V2、W01 与既有修复记录不变；锁文件、依赖清单、Schema、Task/Usage/Hook/Loadout/Scanner 实现均未改动。无新增暂存操作。

基线清单不含 ignored 构建产物；build 正常生成这些产物。未读取真实知识内容或声称扫描了仓库外真实数据目录。索引统计变化已在第 2 节明确，不被隐瞒为纯内部去重。

## 8. 剩余限制与停止点

本项证明的是完整合法 Snapshot 经正常增量同步可正确应用，以及 SQLite 事务内的确定性 SQL 故障回滚和重试恢复；不等于文件系统原子 Snapshot、进程崩溃/断电恢复或多进程确定性验收。F01 YAML/cache、F02 路径检查与 TOCTOU 边界、F03 当前投影资格均保持原实现和既有说明。

F05/U04、F06 watcher 启动窗口、F07 派生索引恢复入口、U02/U03 及真实客户端等未闭环项仍保留。本轮没有自动 rebuild、放宽 READY gate、关闭唯一约束或改写运行历史。

**F04 已实施、自动验证通过，等待用户复核；整体 NO-GO 保留。** N00–N13 实现完成状态不变，既有待用户复核状态不擅自修改。本轮停止，不自动进入 F05/U04。
