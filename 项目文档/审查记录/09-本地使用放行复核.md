# 本地使用放行复核

日期：2026-09-06。对象：当前仓库 HEAD `ab0c9ff14d09f1ca6ae16890ff80fbfbbd663648`。复核开始时工作区干净，采集 144 个 tracked／非忽略 untracked 文件的 SHA-256；代码检查、构建和测试后，这 144 个文件均未变化。

**当前放行状态：GO。2026-09-06，用户在本次复核结果及明确放行范围展示后回复“确认”，接受 §4 的验证限制及 §5 的放行结论。当前 macOS ARM、本地文件系统、个人 Codex 使用范围内的 NO-GO 已解除；不外推为所有环境或全部原清单实测通过。历史待复核记录保留，由本次技术复核及用户放行决定衔接，不改写为逐项历史人工复核。**

依据：[审计修复方案 V2](../设计方案/03-审计修复决策与逐项执行方案-V2.md)、[修复合并记录](07-F04-快照差异与组合移动修复记录.md)、[真实 Desktop 验收记录 §16–§20](../验证记录/02-真实Desktop验收记录.md)。原审计 NO-GO 作为历史结论保留；本报告给出当前基线的复核结果。

## 1. 方法与授权边界

用户授权先进行最终放行复核。单 Agent 核对当前源码、已安装依赖实现、正式回归和本轮真实 Desktop 证据；CodeGraph 用于定位调用链。未修改业务代码、依赖、运行配置或真实知识，未导入旧数据、确认 Asset、执行 Task 终态、停机或再次进行正式库故障注入。测试使用现有隔离夹具，不把夹具中的模拟事件、SQL 或 SDK 调用当成真实 Desktop 事件。

复核判断的是历史缺陷是否仍有阻断证据、当前实现及验证是否足以支持限定范围放行；不是完整重新审计、长期效果评估或对所有并发文件系统竞争作绝对安全承诺。复用本轮真实 Desktop 验收，未要求重新执行用户已豁免的双窗口测试。

## 2. 修复逐项复核

源码路径均相对仓库根；正式测试位于 `apps/server/test/`，ID 测试位于 `packages/id-generator/test/`。表中的“复核通过”是此次技术复核结论。

| 项目 | 当前实现与核对证据 | 本次结论 |
|---|---|---|
| F01／S01 前置信息安全解析 | `asset/frontmatter.ts` 在调用 gray-matter 前要求裸 YAML 分隔符；实际安装的 4.0.3 固定 YAML 引擎调用 safeLoad，显式 options 避免缓存复用和插入。Scanner、Inbox、确认预检复用该解析入口；`frontmatter-security.test.ts` 验证语言头拒绝、执行标记不产生和合法相邻文件保留 | 复核通过；未发现文件选择可执行解析器的原入口复现 |
| F02 当前路径资格 | `asset/scanner.ts` 的 readCandidate／validateCurrentFilePath 对根及相对路径逐层 lstat、realpath，并以 O_NOFOLLOW 打开普通文件；`asset/search.ts` Search／Read 对当前文件、ID、Workspace 重新校验，资格不可确定时拒绝。`path-security.test.ts` 覆盖失效路径和旧 Catalog 负例 | 复核通过；逐层检查不是原子目录遍历，不外推到恶意并发替换父目录的强隔离环境 |
| F03 Loadout 当前投影 | `loadout/service.ts` 的 renderStoredTaskLoadout 经纯 Asset Read 校验当前文件；拒绝项只保留历史身份、不提供动态标题正文；类型改变可临时降为 ON_DEMAND，不重写保存的 Loadout。`loadout-qualification.test.ts` 验证隔离和数据不变性 | 复核通过；历史选择不替代当前资格 |
| F04 快照差异和组合移动 | `asset/catalog.ts` 事务内先删除 removed／invalidated／changed 行，再插入 changed／added，保留唯一约束；`snapshot-difference.test.ts` 覆盖移动、交换、轮换及回滚 | 原用户复核状态保留，本次回归通过 |
| F05／U04 Hook 非阻断与预算 | `hook/user-prompt-submit.ts` 对预期依赖错误返回 0、无上下文，意外异常返回 1；两处 SQLite 读取均采用 100ms 等待。`loadout/renderer.ts` 只对已校验元数据按完整条目降级或省略，不截断摘要或改写保存记录。`hook-resilience.test.ts`、Loadout 测试及真实故障恢复结果相互补充 | 复核通过；真实客户端原始子进程输出／退出码仍未独立采集，不能冒称 D10 原始证据齐全 |
| F06 启动漏扫窗口 | `asset/index-manager.ts` 初扫后等待 Watcher ready，再通过同队列完整复核；启动阶段不提前报告 READY。`startup-readiness.test.ts` 对窗口内新增、删除、Workspace 变更、配置不可用验证 | 复核通过 |
| F07 派生索引重建 | `maintenance-cli.ts` 先要求 --offline 和完整 Snapshot，维护模式只打开既有 DB；`asset/catalog.ts` 在 EXCLUSIVE 事务中仅重建 Catalog／FTS。`maintenance.test.ts` 验证缺失库、锁、损坏结构、事务失败及其他运行表不变 | 复核通过；--offline 仍是操作者对停写的确认，不是自动停止其他进程 |
| U02 Task 终态入口 | 维护 CLI 先只读探测既有数据库，再复用 TaskApplicationService；Repository 条件更新及事务只允许 RUNNING 转终态，保留 Loadout／Usage／Binding | 复核通过；仅隔离测试，未结束真实 Task |
| U03 多进程 ID | 统一生成器使用十进制字符串 `prefix + decimal((Snowflake << 128) | random128)`；格式校验保留旧合法 ID。同毫秒 8 子进程与共享 SQLite 测试通过，消费者仍按字符串使用 | 复核通过；接受的是不透明字符串身份，不承诺可按旧 Snowflake 解码时间或绝对无随机碰撞 |

另核对 `mcp/tools.ts` 从 Task 派生 Workspace，Search／Read 在当前资格检查后记录 Usage，Used 先经纯 Read 检查资格；Resolve 使用纯 Search，Get／List 不调用 Usage 写入。`usage/repository.ts` 的重复 Used 返回既有行，不更新原时间戳。

为后续分批整理入库，补核对 `asset/confirmation.ts`：确切 Inbox 路径和原字节 Hash、COPYFILE_EXCL、不覆盖目标、复制后正式资格与 Hash 检查、源移除前复核、失败补偿。正式 `asset-confirmation.test.ts` 覆盖六种类型／范围组合、Hash 不符、冲突和错误恢复；此项不构成任何真实候选的确认授权。

## 3. 本次实际验证

使用已安装的 Node 22.16.0／pnpm 11.1.3 工具链，无 install／升级。命令通过该工具链 PATH 执行。下表均为本次真实结果，不沿用旧报告计数，也不将源码与 dist 的重复场景累加为独立覆盖数量。

| 检查 | 本次结果 |
|---|---|
| Server 历史缺陷定向源码回归 | 103/103，通过，0 skipped／cancelled |
| Server 相邻契约源码回归 | 51/51，通过，0 skipped／cancelled |
| ID 包测试 | 5/5，通过 |
| `pnpm build` | ID、Server、Hub 三包通过 |
| `pnpm typecheck` | 三包通过 |
| 新构建 Server dist 定向回归 | 102/102，通过，0 skipped／cancelled |

定向源码组使用 `pnpm --filter @codex-memory-os/server exec tsx --test --test-concurrency=1`，文件为 `frontmatter-security`、`path-security`、`loadout-qualification`、`snapshot-difference`、`hook-resilience`、`startup-readiness`、`maintenance`、`multiprocess-identity` 的 `.test.ts`。dist 组加 `CODEX_MEMORY_OS_TEST_DIST=1`，使用前七个文件，动态模块根确认为 dist；没有把未加入的 multiprocess-identity 计入该组。

相邻契约组同一测试入口，文件为 `asset-confirmation`、`mcp`、`loadout`、`usage`、`task`、`hook` 的 `.test.ts`。ID 命令为 `pnpm --filter @codex-memory-os/id-generator test`。本次未重跑全仓完整套件、Hub 浏览器验收和 CLI 脚本模型 smoke；它们的历史结果仍仅作为历史证据。

执行日志和基线保存在仓库外临时目录 `/var/folders/3k/g7_mt00n1rdfxx__dj6vnnk00000gn/T/codex-release-review-ubgeo3p4/`，文件为 `baseline.json`、`targeted-tests.log`、`contracts.log`、`id-tests.log`、`build.log`、`typecheck.log`、`dist-tests.log`。临时日志不保证长期存在，正式测试和本报告中的命令、结果提供可复核入口。

## 4. 客户端门槛如何处理

| 项目 | 证据和处理 |
|---|---|
| 当前真实 Hook、非空 ON_DEMAND、MCP、Usage、attach | 本轮 Desktop 验收已完成，详见验收记录 §16–§17；早期“Desktop 尚未闭环”不能继续无条件当作当前事实 |
| Server 及 Hook 配置故障恢复 | 验收记录 §18–§19 实测通过其注明的行为范围；配置原字节和服务已恢复 |
| 跨 Workspace／双窗口 | 用户已明确豁免 Desktop 项；本次 MCP、路径及 Task 自动回归提供代码边界证据，但不替代真实双窗口验收，不再次索要同一豁免 |
| GLOBAL 与 MEMORY DIRECT 摘要 | MCP 测试验证 GLOBAL＋本 Workspace、null 仅 GLOBAL；Hook 测试验证 GLOBAL MEMORY 的 DIRECT 摘要及无 Usage 副作用。真实 Desktop 仍未覆盖，作为限定放行的未覆盖项提交接受 |
| 未预先泄露正文值的真实模型实验 | 未完成；当前真实 Read 返回及计数已验证，未宣称长期质量或减少 Read 效果。该效果实验不作为本地按需读取与人工整理的必要门槛 |
| 非空 List 零副作用 | 真实 Desktop 尚未专项验证；当前实现是读取 Task／Loadout，相关 MCP／Loadout 自动测试已覆盖。不能写成真实 Desktop 六工具细项无例外全部通过 |
| Hub／环境 | 复用 U05 的历史本机浏览器结果，本次构建／类型检查通过；其他 OS、网络盘、极端规模、长期无人值守仍未验证 |

上述未覆盖项没有被默认为用户新豁免。技术判断是：现有源码与自动测试，加上本轮真实链路和故障恢复，足以建议当前受限用途放行；若用户要求原清单所有细项实测，则仍需另行补足相应客户端证据。

## 5. 用户已确认的放行结论

2026-09-06，用户已接受以下完整结论，当前放行状态记录为 GO：

> 在 HEAD `ab0c9ff14d09f1ca6ae16890ff80fbfbbd663648` 的本次复核基础上，允许 CodexMemoryOS 用于当前 macOS ARM、本地文件系统、个人 Codex 的知识读取与显式装配，并分批开展旧知识整理及人工确认入库。接受 §4 所列证据边界；跨 Workspace／双窗口沿用既有用户豁免。此结论不承诺团队多租户安全、其他环境或长期效果，不授权自动批量确认、删除旧系统、覆盖旧数据或扩大网络暴露。真实 Asset 仍逐文件绑定当前 Hash 确认。

确认来源：Desktop Session `01a07517-f47b-7893-8b6e-7400202a9cdb` 中，助手展示复核结论并询问是否接受当前 macOS、本地文件系统、个人 Codex 使用及分批整理、逐文件确认入库范围，用户明确回复“确认”。该确认接受本次放行范围和残留验证限制；跨 Workspace／双窗口沿用此前豁免，不增加虚构的实测结果。原审计和修复历史保留。

本轮尚未开展 M00–M05。GO 也不意味着已取得旧数据源目录读取、候选生成或正式确认的全部授权；后续按已定义的人工整理步骤和用户给定范围执行。
